---
apiVersion: v1
kind: Service
metadata:
  name: shipping1
  labels:
    name: shipping1
  namespace: sock-shop
spec:
  ports:
    # the port that this service should serve on
  - port: 80
    targetPort: 80
  selector:
    name: shipping1

